(import cstdio)
(import macros)
(import operator-macros)

(def main (fn extern-c int (void)
  (def x (var auto (array-of (+ 1 2 3) int) (array 0 1 2 3 4 5)))
  (for (i \ 0) (< i 6) (incv i)
    (printf "%d\n" (@$ x i)))
  (setv x (array-of (+ 1 2 3) int (array 1 2 3 4 5 6)))
  (for (i \ 0) (< i 6) (incv i)
    (printf "%d\n" (@$ x i)))
  (def y (var auto int (+ 1 2 3)))
  (printf "%d\n" y)
  0))
