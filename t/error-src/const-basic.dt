(import cstdio)

(using-namespace std

(def main
  (fn extern-c int (void)
    (def n (var auto (const int) 100))
    (def pn (var auto (const (p (const int))) (# n)))
    (setv n 200)
    (setv pn (# n))
    (setf pn 200)
    

    0))


)
